#include <gtest/gtest.h>
#include <gmock/gmock.h>
#include <tinytp/cli.h>

using namespace ::testing;
using namespace tinytp;


TEST(CliTestSuite, ParseCollect) {
    ASSERT_TRUE(true);
}

TEST(CliTestSuite, ParsePrio) {
    ASSERT_TRUE(true);
}