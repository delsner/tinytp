#include <tinytp/report-parser.h>

namespace tinytp {
    std::vector<TestSuiteExecution> JUnitParser::parse() {
        std::vector<TestSuiteExecution> executions;
        // TODO Parse junit XML file
        return executions;
    }
}
