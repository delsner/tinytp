#include <tinytp/prio.h>

namespace tinytp {
    int TinyTPPrio::run() {
        // Choose TP algo with DB
        return 0;
    }

    std::vector<std::string> HistoryPathDurationTP::prioritize() {
        // Query db with SQL
        // group by module names if necessary
        return {};
    }

}

